{
  "name": "yfy-qa-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "mock": "node --experimental-strip-types dev/mock.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250109.0",
    "@types/node": "24.10.1",
    "typescript": "5.9.3",
    "wrangler": "3.99.0"
  }
}
